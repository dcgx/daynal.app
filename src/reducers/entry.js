/*
  {
    entries: [],
    isActive: null,
  }
*/

const initialState = {
  entries: [],
  isActive: null,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
